import { Layout } from 'antd'
import React from 'react'

const { Footer } = Layout

const AppFooter: React.FC = () => {
  return <Footer style={{ textAlign: 'center' }}>Demo Site</Footer>
}

export default AppFooter
